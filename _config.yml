markdown: GFM
title: Steven Yuan's Repository
description: USC coursework, personal projects, and movie reviews.

# Disable default theme — we use a custom layout instead
theme: null

defaults:
  - scope:
      path: ""
    values:
      layout: default
